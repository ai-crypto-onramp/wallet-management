name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: ai-crypto-onramp/ai-crypto-onramp/workflows/go-service-ci.yml@main
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
